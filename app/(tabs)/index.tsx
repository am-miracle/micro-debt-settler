import { ScrollView, Text, View } from "react-native";

export default function HomeScreen() {
  return (
    <ScrollView>
      <View>
        <Text className="text-3xl bg-red-900">Working</Text>
      </View>
    </ScrollView>
  );
}
